Speaker 1  0:01  
Okay, now with regards to how the how the app must actually work, so I'm thinking about about doing a like like something that can run locally on someone's laptop. It doesn't have to be deployed, and it must be relatively fast. So using something like Go or Rust, so it's not massive in in shipping size or like in like sending it to someone. But it would but in terms of the front end of the app, it must have a web front end. It's okay if it has a desktop font, then if that's easier to code, but preferably some web interface with some buttons and interactivity. But all must must be like run locally on the on the person's laptop when it's simulating all these all these all these claims. Okay, in choosing the framework, the tech stack, it's important to consider the packages available. We need to use like we we need to simulate like we need simulation packages. So that is, for example, what do you call it? Like it needs compound distributions and something like a compound Poisson or I can't remember log. I think there's something about log log normal for the for the for the loss size and and things like that. So we need to be able to parameterize. We need we need nice packages available. So so if Go has good packages that that you can use to do compound compound distributions, then that's great. Or if if if Rust has it, that's also good. Rust seems like a good fit because we don't need parallelization and stuff like that, really. So so yeah. So, so although parallelization could be useful if if we're simulating multiple classes at once, but that is quite a simple thing to to sort of dispatch. So, yeah. yeah, okay. So so so I'm rambling a bit here, but so don't take all these comments too seriously. That this will be refined in the next phase, in the design phase. But with regards to how the actual simulation of the data should work, it's it's really important that for every claim that's being simulated, it has a realistic exposure measure, and the so so the exposure measure being in all the possible policies that can be claimed for, or like that that that claims can rise. And so let's just take motor insurance as an example, and let's just first focus on motors and motor insurance. So yeah, and let's do that for simplicity. Later on, we can maybe add property like commercial property insurance or some or something like that. But let's so yeah. So let's just focus on maybe personal motor insurance. So we have a dataset. Okay. So we first need to simulate the like all the possible policies, like all the policies that are in our dataset. Okay, so that is all the policies that the insurer have sold business to all the all the customers, and based on this, so we need to simulate values of like some insurance values of vehicles. So let's do it in dollars, dollar amounts, and estimate what what the different vehicles vehicles are. So also one customer. Customer, let's let's make the simplifying assumption that a customer only has one vehicle, just for simplicity, and that if there are two vehicles, that's that's that's two. Two policies. Okay, let's randomize the the the and simulate the the sizes of vehicles or like the the the sum insured of vehicles, and also we. maybe design a like a sort of like a risk factor associated to to every vehicle that can be used as a loading for how how likely that vehicle is to claim. Okay, and then based on based on that information, okay. So, so first we need to come up with an algorithm to simulate all the all the policies, okay. So that'll be like step one, and and after we've yeah, and and as parameters to this to this app, we need we need to like we need to be able to specify the size of the of the book, so how many policies there are, and we need some parameters to sort of play around with maybe the the the volatility. So not volatility, but the but the like the standard deviation of the the distribution.

Speaker 1  6:38  
So so so so if the we need some kind of parameter that, if we specify that it's the parameter is large, we know it's like the book is very volatile. But if the parameter is small, it's it's quite homogeneous in the sense that all the sum insured and the risk factor and the riskiness associated to every policy is very similar. Okay, so if we can use those two parameters, then that'll make it possible to reuse the same code for other simulating other lines of business, not just motor personal. Okay, so that that I feel like that's a good step one, and then we need we need some other like the the next phases will involve simulating the actual like the claim events and the certain time and dates associated with them, based on this book of information. So this this policy data set, and then based on and then after we've simulated simulated the claim events, I think I think it makes sense that yeah. So so after simulating the claim events, we use the information, like the policy information about every every claim. So, looking at the sum insured, looking at at various things, we we then use that information and then also simulate what the transactions are. But when every claim is is closed, we want the we want the the the cumulative case estimate to to converge to zero. Okay, so it's very important that every claim, every transaction is is like every every claim is being finalized, or at least that that the total paid amount and the total case estimate amount converges at the end. So if we if we if we think about case estimate as it's the amount that's outstanding, that that amount of that must converge to zero when the when the when the claim is closed. Okay, so okay, it's a bit confusing, maybe. But jumping back to the to the to the dissimulating the claim events, we need to simulate. I think it makes sense to simulate the the claim start and end dates. Consider that every policy can can potentially claim more than once, but as soon as there's a massive claim, yeah, okay, okay. Just assume that every policy can claim more than once independently. So, okay, cool. So, so, and then we simulate the the open like the the the date of occurrence, the date of occurrence and the date of report is really is must also be a parameter of how close they can like how how big that lag can be, for example in motor motor insurance, motor personal that. That lag won't be very long, maybe like a maximum of not not a hard maximum, but but on the on the upper end of the scale it should be about 30 days. Maybe like they can be outliers for it to be a bit bit more, but on the but most of them will be reported within a day or two, so that's the difference between the date of occurrence and the report date. And then, on the on the on the close date, we need to simulate some like. like some time time delay. So I'm thinking about maybe using a a exposure like a like a like a queue a queuing like an event process. So every so like a exponential distribution might be able to work, and then that exponential distribution could maybe be be parameterized. Also, like a see, we set some global parameter for the given class of business, but that that global parameter is then adjusted with a formula based on the the the sum insured value and like policy details, so like the it uses the the sum insured and maybe the riskiness of the policyholder. So it's a it applies a formula to that to to set how how long the the the claim will take out to pay, and I think it should also be correlated to the size, to the initial size of the claim. Okay, so okay, so jumping back to also the the the claims that I said. So when a claim is initially reported, we also need to simulate a estimated size of the loss. Okay, so the estimated size of the loss must be. Let's see. So estimated size of the loss must be related to the sum insured, but some claims will be will be small.

Speaker 1  12:23  
Some claims will be claims will be large, and we also need a a excess. So so something which which caps the size. Like it's a limit. Like claims must be larger than a than than a certain size, so that must also is also something that that must be captured in the in the in the policy data set that we simulate. The excesses are typically like multiples of $100 Okay, so we need to simulate. Well, that's going to be hard, but we also need need to simulate what the excess is per like configured per policy. So and that excess is typically not a like a like it's it. Let's assume it's a multiple of $100 So yeah, so it can be like $500 excess or $1,000 excess, or you know maybe they can choose to maybe set it to like yeah. So oh, actually yeah, let's simplify it. Let's assume that the excesses can either be $100 $0 So it's $0 $100 $300 $500 and $1,000 Let's just assume simple multiples for. for a given class of business. So so these possible excesses, choices of excess are are are also like it's almost like a parameter per per line of business. Business. Okay, so okay, so then, what else do we need? So we need. Okay, so then also based on this information. we need to okay so okay so so then using all this information when a claim is reported the claim cannot be. Smaller than the excess because then that claim is not is not reportable, and we we need to come up with with a with an initial estimate of what the possible claim loss is going to be. Okay, that needs to be randomly generated. So. we can maybe use a distribution. Maybe make a recommendation of a distribution that we can use to simulate that. But maybe something like a log normal could could work, or a Pareto. And again, the parameters of that must be configurable per per line of business. So in this case, we start off with just using motor insurance, make it a parameter so that this is extendable to other lines of business easily later on when we expand the app. Okay, then based on this information. We need to simulate how how the claims run run off. Okay, so how they are paid out over time. Larger claims. Yeah. So I'm tempted to make the okay no so ignore that, so let's let's just assume that claims that like small claims large claims they all can take take short or long okay no no actually that doesn't make sense smaller smaller initially reported claims should, on average, close a bit quicker than the large claims. Okay, larger claims claims tend to take a bit longer to to close. So we need to, based on this this claim report information, we need to simulate what what the close dates are. Okay, so yeah, as I said earlier, maybe like an exponential distribution, like a something like that could can maybe work, or or it might be a Poisson. Yeah, I think it's an exponential, yeah, exponential distribution. So it's not a compound Poisson distribution; exponential. Okay, it's like a queue, like a weight process, and there's only one event. Okay, cool. Yeah, I might be wrong with the distribution there, but make a recommendation on what the distribution can be for simulating the lag between the current state or the yeah the the the the report date and the close date. Okay, but it's important, as I said earlier, to consider that the claim size, the initial estimated claim size and policy information is is incorporated in parameterizing what that lag is. Okay, cool. So, based on that information, we can now simulate the transactions, the the claim transactions. So the the payments, payment transactions have dates, and they each one has has an amount. So these amounts must be quite like realistic. So they must not go beyond what the what the cumulative case estimate is is at that point. So then maybe it makes sense to first simulate the the case estimate over the lifetime of the project. So let's simulate the case estimate based on based on the information, which is the initial case estimate, the initial case estimate. So we're now going to simulate the case estimate runoff.

Speaker 1  19:21  
Okay, I think some like survival process could work well, where we model the like maybe the cumulative survival of a claim, or the, or the, yeah, like, or maybe it can can like decay, and it must converge to zero. So, so it can either jump up a bit, or it can jump down, based on some parameters. But it must end at at. At zero, so that simulating that like that math can be quite tricky, but we need some some form of randomness in there, but perhaps perhaps combined with some bespoke logic. Yeah, so that basically just the requirements are that the initial case estimate must be must be yeah. So the niche case estimate is used, and then maybe we we want to see how that case estimate grows or or decays or like gets paid out, like becomes smaller over time, and then based and then it must at the end when when the claim is closed converge to zero. Okay, now based on that information, we can derive what the paid amounts were. I think there's no randomness needed there. We can simply assume that the payments. Yeah, we can just derive simple assumptions, like like for example, if the case estimates started out at at $10,000 $10,000 Then a couple days later, there was a a case estimate movement that made it a bit that made it quite smaller. Maybe it was an initial payment made, and so that's the outstanding case estimate is now smaller, and that then there's an that that just means that there's a a a a pay transaction with exactly the the value of that gap, okay, associated with it, and then there's other days where the case estimate might might grow larger, okay, and there's no, so we have all that information in the case estimate. So, yeah, so so then whenever the case estimate becomes smaller, we assume it's there was a payment done, but not in all cases, okay. Actually, so that means there must be some random component to the to the payments, okay. So yeah, I don't know completely how we're going to do that yet. Maybe you can come and come up with a recommendation.